{
  "name": "retail-api-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "build": "babel -d build/ src/",
    "prebuild": "rimraf build/",
    "start": "node build/server.js",
    "prestart": "npm run build",
    "dev": "nodemon --watch src/server.js --exec babel-node src/server.js",
    "lint": "eslint .",
    "test": "jest"
  },
  "author": "Nasir Abbas",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "debug": "^4.1.1",
    "express": "^4.16.4",
    "mongoose": "^5.4.14",
    "rimraf": "^2.6.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^24.1.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.14.1",
    "jest": "^24.1.0",
    "nodemon": "^1.18.10",
    "supertest": "^3.4.2"
  }
}
